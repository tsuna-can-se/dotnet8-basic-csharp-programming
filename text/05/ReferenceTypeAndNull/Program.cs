string name = "ツナ缶";
string empty = "";
string hoge = null; // 明示的に null を指定可能。

// 以下のように、何も代入していない参照型の変数の既定値は null 。
// string hoge;

Console.WriteLine(name);
Console.WriteLine(empty);
Console.WriteLine(hoge);
